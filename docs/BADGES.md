# Badges

The README uses shields for:

- CI status
- GHCR package
- GitHub releases
- Python version
- MIT license

Package and release badges become more useful after:

1. the `package` workflow publishes `ghcr.io/chathurangabw/intake`, and
2. the first version tag, for example `v0.1.0`, creates a release.
